{
  "name": "ambient-mode",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^3.2.3"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
